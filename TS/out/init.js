"use strict";
// Пример инициализации объектов
const STANDART_NORMAL = { x: 0, y: 0 };
let df = { a: 22, b: 11 };
let { a: p, b: o } = df;
console.log({ prosto: p, offigeti: o });
console.log("Привет Олег!");
